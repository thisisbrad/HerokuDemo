{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.14.0",
    "npm": "8.3.1"
  },
  "scripts": {
    "heroku-postbuild": "npm install --prefix api && npm install --only=dev --prefix reactjs && npm install --prefix reactjs && npm run build --prefix reactjs",
    "dev": "concurrently --raw \"npm run reactjs\" \" npm run api\"",
    "reactjs": "cd reactjs; npm start",
    "api": "cd api; node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
